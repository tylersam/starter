<%= form_for(@approved_user) do |f| %>
  <% if @approved_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@approved_user.errors.count, "error") %> prohibited this approved_user from being saved:</h2>

      <ul>
      <% @approved_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :role %><br>
    <%= f.number_field :role %>
  </div>
  <div class="actions">

    <%= f.submit class: 'btn btn-success' %>
    <%= link_to 'Back', approved_users_path, class: 'btn btn-default' %>
  </div>
<% end %>
